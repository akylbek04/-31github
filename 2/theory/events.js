// const Emitter = require("events");

// const emitter = new Emitter();


// const callback = (data, second) => {
//   console.log(`You sent the message: ${data}`);
//   console.log(`DATE: ${second}`);
// };

// emitter.once("message", callback);

// const msg =  "Hello World!!!";

// if (msg) {
//   emitter.emit("message", msg, new Date());
// } else {
//   emitter.emit("message", "NO MESSAGE", new Date());
// }

// emitter.removeListener("message", callback)

